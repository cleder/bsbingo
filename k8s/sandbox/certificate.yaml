apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cluster-cert
spec:
  secretName: cluster-cert-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - api.sandbox.bs-bingo.app
    - k8s.sandbox.bs-bingo.app
    - nextjs.bs-bingo.app
